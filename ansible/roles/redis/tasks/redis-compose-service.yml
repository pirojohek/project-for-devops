- name: Закидываем Redis Systemd unit-файл
  template:
    src: redis-compose.service.j2
    dest: "/etc/systemd/system/{{ service_name }}.service"
    owner: root
    group: root
    mode: '0644'
  notify:
    - Reload system
    - Enable and start Redis

